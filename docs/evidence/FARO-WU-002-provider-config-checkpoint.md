# FARO-WU-002 — Provider Configuration Checkpoint

Date: 2026-08-30

`FARO_WU002_PROVIDER_CONFIG=PARTIAL_BLOCKED`

## Vercel

- connected team: `team_HECWodzpFDWJQCxmo9ZUwmv5`
- user reports FARO project was created manually in Vercel
- fresh provider readback still lists only project `brasko`
- direct lookups for `faro-financeiro`, `faroapp1`, and `faro` returned 404
- no duplicate project was created by the agent
- no deployment was triggered by the agent
- production promotion remains denied

Verdict: B2 remains IN_PROGRESS until the manually created project identity is observable by project slug/ID in the connected Vercel scope.

## Supabase

- project `faro-financeiro` / `mjbyqhreptllilkggiri` is `ACTIVE_HEALTHY`
- commercial Edge Functions remain ACTIVE:
  - `criar-checkout-faro`
  - `abrir-portal-faro`
  - `stripe-webhook-faro`
- function code remains fail-closed when required Stripe/runtime variables are absent
- expected server-side variables:
  - `STRIPE_SECRET_KEY`
  - `STRIPE_FARO_MONTHLY_PRICE_ID`
  - `FARO_APP_URL`
  - `STRIPE_WEBHOOK_SECRET`
- Edge Function Secrets management is not exposed by the connected Supabase tool
- attempted secure Vault backup of the newly-created webhook signing secret failed because the Postgres connection timed out; no secret was written to Git or evidence

## Stripe — Brasko Agency sandbox

- account context: `acct_1UAESjLsq19Xe9NB`
- FARO product/price/promotion remain canonical from the prior receipt
- new webhook endpoint: `we_1UAFofLsq19Xe9NBk4gSh2tW`
- webhook target: `https://mjbyqhreptllilkggiri.supabase.co/functions/v1/stripe-webhook-faro`
- endpoint status: enabled
- exactly six events enabled:
  - `checkout.session.completed`
  - `customer.subscription.created`
  - `customer.subscription.updated`
  - `customer.subscription.deleted`
  - `invoice.paid`
  - `invoice.payment_failed`
- signing secret was generated by Stripe but is intentionally absent from this receipt
- Stripe API exposes no operation for creating/exporting restricted API keys
- Billing Portal configurations in this sandbox: 0
- Stripe API exposes read/list for Billing Portal configurations but no create operation in the connected surface

## Guards preserved

- no Stripe live-mode mutation
- no Vercel production deployment
- `billingEnabled=false`
- no secrets in Git/frontend/evidence/logs
- no duplicate Vercel project created while provider identity is ambiguous

## Next proof target

1. Resolve the exact Vercel project slug/ID created manually and read it back through the connected provider.
2. Use that project URL as `FARO_APP_URL` for sandbox E2E.
3. Install the sandbox restricted Stripe key + webhook signing secret directly into Supabase Edge Function Secrets outside chat.
4. Configure the Stripe Billing Portal in sandbox.
5. Then prove B6-B8 end-to-end.
